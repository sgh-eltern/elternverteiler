<% form(@erziehungsberechtigter,
        :inputs_wrapper => :fieldset,
        class: 'pure-form pure-form-aligned',
        action: "/eltern/#{@erziehungsberechtigter.id}",
        method: 'post',
        ) do |f| %>
  <% f.inputs(legend: 'Erziehungsberechtigter') do %>
    <div class="pure-control-group">
        <label for="vorname">Vorname</label>
        <%== f.input(:vorname, size: 50) %>
    </div>

    <div class="pure-control-group">
        <label for="nachname">Nachname</label>
        <%== f.input(:nachname, size: 50) %>
    </div>

    <div class="pure-control-group">
        <label for="mail">eMail</label>
        <%== f.input(:mail, size: 50) %>
    </div>

    <div class="pure-control-group">
        <label for="telefon">Telefon</label>
        <%== f.input(:telefon, size: 50) %>
    </div>
  <% end %>
  <%== f.button(value: 'Aktualisieren', class: "pure-button pure-button-primary") %>
  <input type="reset" value="Zurücksetzen" class="pure-button">
<% end %>
