<% form(@erziehungsberechtigung,
        :inputs_wrapper => :fieldset,
        class: 'pure-form pure-form-aligned',
        method: 'post',
        action: "/schueler/#{@erziehungsberechtigung.schüler.id}/erziehungsberechtigter/add",
        ) do |f| %>
  <%= @erziehungsberechtigung.schüler %>
  <%== f.input(:erziehungsberechtigter, size: 1, wrapper: :div, wrapper_attr: {class: 'pure-control-group'}, labeler: :explicit) %>
  <%== f.button(value: 'Speichern', class: "pure-button pure-button-primary") %>
  <input type="reset" value="Zurücksetzen" class="pure-button">
<% end %>
