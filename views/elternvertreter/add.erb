<% form(@amtsperiode,
        :inputs_wrapper => :fieldset,
        class: 'pure-form pure-form-aligned',
        method: 'post',
        action: "/klassen/#{@amtsperiode.klasse.id}/ämter/add",
        ) do |f| %>
  <%# TODO Do not show all parents, but only parents from @amtsperiode.klasse %>
  <%== f.input(:amt, wrapper: :div, wrapper_attr: {class: 'pure-control-group'}, labeler: :explicit) %>
  <%== f.input(:inhaber, wrapper: :div, wrapper_attr: {class: 'pure-control-group'}, labeler: :explicit) %>
  <%== f.button(value: 'Speichern', class: "pure-button pure-button-primary") %>
  <input type="reset" value="Zurücksetzen" class="pure-button">
<% end %>
