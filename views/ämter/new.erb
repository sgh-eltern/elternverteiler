<% form(@amt,
        :inputs_wrapper => :fieldset,
        class: 'pure-form pure-form-aligned',
        action: "/ämter",
        method: 'post',
        ) do |f| %>
  <%== f.input(:name, label: 'Name', size: 50) %>
  <%== f.button(value: 'Anlegen', class: "pure-button pure-button-primary") %>
<% end %>
