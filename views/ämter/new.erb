<% form(@amt,
        :inputs_wrapper => :fieldset,
        class: 'pure-form pure-form-aligned',
        action: "/ämter",
        method: 'post',
        ) do |f| %>
  <%== f.input(:name, size: 50, wrapper: :div, wrapper_attr: {class: 'pure-control-group'}, labeler: :explicit) %>
  <%== f.input(:mail, size: 20, wrapper: :div, wrapper_attr: {class: 'pure-control-group'}, labeler: :explicit) %>

  <%== f.button(value: 'Anlegen', class: "pure-button pure-button-primary") %>
<% end %>
